'use client';

import CraneMachine from '../../crane-machine/page';
import { CAMEL_THEME } from '../../lib/themes';

export default function CamelCraneMachine() {
  return <CraneMachine theme={CAMEL_THEME} />;
}
