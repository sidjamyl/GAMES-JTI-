'use client';

import StackTower from '../../stack-tower/page';
import { CAMEL_THEME } from '../../lib/themes';

export default function CamelStackTower() {
  return <StackTower theme={CAMEL_THEME} />;
}
