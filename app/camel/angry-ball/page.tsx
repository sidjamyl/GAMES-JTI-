'use client';

import AngryBall from '../../angry-ball/page';
import { CAMEL_THEME } from '../../lib/themes';

export default function CamelAngryBall() {
  return <AngryBall theme={CAMEL_THEME} />;
}
