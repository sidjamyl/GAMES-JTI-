'use client';

import GiftCatcher from '../../whac-a-mole/page';
import { CAMEL_THEME } from '../../lib/themes';

export default function CamelGiftCatcher() {
  return <GiftCatcher theme={CAMEL_THEME} />;
}
