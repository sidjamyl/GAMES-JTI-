'use client';

import GiftSlice from '../../gift-slice/page';
import { CAMEL_THEME } from '../../lib/themes';

export default function CamelGiftSlice() {
  return <GiftSlice theme={CAMEL_THEME} />;
}
